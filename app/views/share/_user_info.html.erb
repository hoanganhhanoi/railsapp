
<%= link_to avatar_user(current_user,50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to "Xem thong tin", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>